import React from "react";

const ScheduleContainer = ({ bestSolution }) => {
  console.log(bestSolution);
  return (
    <div style={{ backgroundColor: "blue", margin: 10 }}>ScheduleContainer</div>
  );
};

export default ScheduleContainer;
